# Assets

- **images/** – app images (e.g. logos, illustrations, photos)
- **icons/** – icon assets (e.g. tab icons, UI icons)

Use with React Native:

```ts
import logo from '../assets/images/logo.png';
<Image source={logo} style={{ width: 100, height: 100 }} />
```
