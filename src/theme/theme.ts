import { lightTheme, type Theme } from './colors';

/** Active theme - light for development. Switch to darkTheme when dark mode is enabled. */
export const appTheme: Theme = lightTheme;

// To use dark theme: import { darkTheme } from './colors'; then export const appTheme = darkTheme;
